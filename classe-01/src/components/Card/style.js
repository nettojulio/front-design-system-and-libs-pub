import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    root: {
        width: 330,
        height: 'auto',
        marginBottom: 20,
        marginTop: 40
    },
    media: {
        height: 'auto',
        width: '100%',
        padding: '0px 10px'
    },
    cardHeader: {
        textAlign: 'center'
    }
}));